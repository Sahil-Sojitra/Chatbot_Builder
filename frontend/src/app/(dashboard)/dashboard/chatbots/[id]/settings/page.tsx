import { EmptyState } from "@/components/ui";

export default function ChatbotSettingsPage() {
  return (
    <EmptyState
      title="Settings coming soon"
      description="Publishing, provider/model configuration, and RAG settings will live here once they're built."
    />
  );
}
