import { EmptyState } from "@/components/ui";

export default function KnowledgeSourcesPage() {
  return (
    <EmptyState
      title="Knowledge sources coming soon"
      description="Connect docs, URLs, and files your chatbots can use once this section is built."
    />
  );
}
